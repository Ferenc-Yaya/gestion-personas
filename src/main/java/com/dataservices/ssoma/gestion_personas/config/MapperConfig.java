package com.dataservices.ssoma.gestion_personas.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MapperConfig {
    // Esta clase es para configuraciones adicionales de MapStruct si son necesarias
    // Por ahora está vacía ya que MapStruct se autoconfigura con @Mapper(componentModel = "spring")
}
